

# Linux Permissions

**Name**: Jan Freitag   
**Klasse**: 4AHITS   
**Fach**: ITSE   
**Datum**: 11.05.2026   
**Anweisungen**: [Linux Permissions](https://www.franzmatejka.at/htl/doc/ITSI_3/lab/15_permissions_ue.html)

## Vorbereitung der Übungsumgebung

###### Script:

```bash
#!/bin/bash

# Gruppen anlegen
groupadd music
groupadd wrestle
groupadd emperors

# User anlegen und Gruppen zuweisen
useradd -m -G music elvis
useradd -m -G music,wrestle blondie
useradd -m -G wrestle hogan
useradd -m -G emperors nero

# Passwörter auf 'kali' setzen
echo "elvis:kali" | chpasswd
echo "blondie:kali" | chpasswd
echo "hogan:kali" | chpasswd
echo "nero:kali" | chpasswd

# Lab-Struktur vorbereiten
mkdir -p /tmp/lab/gigs /tmp/lab/drafts
echo "Draft" > /tmp/lab/drafts/lyrics.txt
echo "Plans" > /tmp/lab/wrestle_plans.txt
chown -R root:root /tmp/lab
chmod -R 755 /tmp/lab
```

Das Script erstellt user, welche dann auch selbst erstellten Gruppen zugeordnet werden. Das Passwort ist für alle user `kali`. Der Letzte Abschnitt erstellt im tmp-Ordner eine Ordnerstruktur mit files und berechtigungen um die Übung möglich zu machen.



## Übungen

### Übung (Analyse und Symbolic Mode)

1. ```shell
   ┌──(kali㉿Kali)-[/tmp/lab]
   └─$ ls -l wrestle_plans.txt 
   -rwxr-xr-x 1 root root 6 May 11 11:59 wrestle_plans.txt
   ```

2. ```shell
   ┌──(kali㉿Kali)-[/tmp/lab]
   └─$ sudo touch notes.txt                                                                                                                                                                            
   ┌──(kali㉿Kali)-[/tmp/lab]
   └─$ sudo chmod o-r notes.txt
   ```

3. ```shell
   ┌──(kali㉿Kali)-[/tmp/lab]
   └─$ sudo chmod g-rw notes.txt
   ```

4. ```shell
   ┌──(kali㉿Kali)-[/tmp/lab]
   └─$ sudo chmod ug+x myscript.sh 
   ```

5. ```shell
   ┌──(kali㉿Kali)-[/tmp/lab]
   └─$ sudo chmod go-wx data.txt
   ```



### Übung (Ownership und Gruppenkollaboration)

1. ```shell
   ┌──(kali㉿Kali)-[/tmp/lab]
   └─$ sudo su                  
   ┌──(root㉿Kali)-[/tmp/lab]
   └─# visudo /etc/sudoers
   ```

   In /etc/sudoers `blondie ALL=(root) /usr/bin/mv, /usr/bin/chown` einfügen

2. ```shell
   $ touch song.txt
   $ sudo mv song.txt /tmp/lab
   $ sudo chown :music /tmp/lab/song.txt
   ```

3. ```shell
   ┌──(root㉿Kali)-[/tmp/lab]
   └─# su - blondie
   $ pwd
   /home/blondie
   ```

   ```shell
   ┌──(root㉿Kali)-[/tmp/lab]
   └─# su blondie  
   $ pwd
   /tmp/lab
   ```

   ohne dem `-` bleibt man im gleichen Ordner

4. ```shell
   ┌──(root㉿Kali)-[/tmp/lab]
   └─# chown elvis song.txt
   ```

5. In `/etc/sudoers` `hogan ALL=(root) /usr/bin/touch, /usr/bin/chown, /usr/bin/chmod` einfügen.

   ```shell
   ┌──(root㉿Kali)-[/tmp/lab]
   └─# su - hogan
   $ sudo touch training.txt
   $ sudo chown :wrestle training.txt
   $ sudo chmod g+rw training.txt
   $ sudo chmod o-r training.txt
   ```

6. ```shell
   ┌──(root㉿Kali)-[/tmp/lab]
   └─# su elvis
   $ chown :emperors song.txt
   chown: changing group of 'song.txt': Operation not permitted
   ```

### Übung (Directory Permissions und Octal Notation)

1. `rwxr-xr-x` → `chmod 755`

   `rw-rw----` → `chmod 660`

   `rwx------` → `chmod 700`

2. ```shell
   ┌──(kali㉿Kali)-[~]
   └─$ mkdir pub
                                                                                                                                                                                                                     
   ┌──(kali㉿Kali)-[~]
   └─$ chmod 751 pub
   
   ```

3. ```shell
   ┌──(kali㉿Kali)-[/tmp/lab]
   └─$ sudo chmod -R 760 drafts
   
   ┌──(kali㉿Kali)-[/tmp/lab]
   └─$ sudo chown -R :music drafts
   ```

4. ```shell
   ┌──(kali㉿Kali)-[/tmp/lab]
   └─$ chmod 711 /home/kali/
   ```

5. `600` sagt, dass other keine execute rechte hat. Für cd braucht man diese aber. Deshalb in 4. `711` 



### Übung (Webserver Directory Permissions)

### other/read Berechtigung entfernen

```shell
┌──(kali㉿Kali)-[/var]
└─$ sudo chmod o-r www
```



### Testen, ob man `www` noch lesen kann

```
┌──(kali㉿Kali)-[/var]
└─$ cd www
                                                                                                                                                                                                                  
┌──(kali㉿Kali)-[/var/www]
└─$ ls        
ls: cannot open directory '.': Permission denied
```

`cd` nachwievor möglich, da other immer noch executen darf

```shell
┌──(kali㉿Kali)-[/var/www]
└─$ cd html
```



### +r-x Auswirkungen

```shell
┌──(kali㉿Kali)-[/var]
└─$ sudo chmod o+r-x www
```

Man könnte sich jetzt theoretisch wieder den Inhalt anschauen, aber kann nicht mehr per `cd` rein um ls überhaupt ausführen zu können.



### Übung (Webserver Directory Permissions II)

### dir erstellen

```shell
┌──(kali㉿Kali)-[/var/www]
└─$ sudo mkdir data                 
```



### `data` der Gruppe `www-data` zuweisen

```shell
┌──(kali㉿Kali)-[/var/www]
└─$ sudo chown :www-data data  
```



### Berechtigungen setzen

```
┌──(kali㉿Kali)-[/var/www]
└─$ sudo chmod 734 data 
```



### Sticky-Bit setzen

```
┌──(kali㉿Kali)-[/var/www]
└─$ sudo chmod +t data 
```



### GID setzen (files im dir sind auch von der gleichen Gruppe)

```
┌──(kali㉿Kali)-[/var/www]
└─$ sudo chmod +s data
```

